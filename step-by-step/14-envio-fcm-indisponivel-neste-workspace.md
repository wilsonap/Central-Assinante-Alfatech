# Envio FCM — indisponível neste workspace

**Data:** 08/08/2026  
**Ação:** implementação data-only **não realizada** (código de envio ausente).

Busca por `fcm.googleapis.com`, `messages:send`, server key, service account send, etc. no repo `Central-Assinante-Alfatech`: **nenhum emissor FCM**.

Este projeto só contém o app Android (recepção + registro de token).  
Nenhuma alteração foi feita no Android.
